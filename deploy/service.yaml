apiVersion: v1
kind: Service
metadata:
  name: ai-kustomize-agent-api
  namespace: devops-tools
  labels:
    app: ai-kustomize-agent
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: ai-kustomize-agent
